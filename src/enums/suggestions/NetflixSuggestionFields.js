/**
 * Possible fields when searching suggestions
 */
const NetflixSuggestionFields = {
  LENGTH: 'length',
  REFERENCE: 'referenceId',
  TRACKS: 'trackIds',
}

export default NetflixSuggestionFields
